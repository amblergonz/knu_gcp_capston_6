{
  "name": "@hover/ingestion-api",
  "version": "1.0.0",
  "description": "Ingestion API - Event collection endpoint",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "dev": "node --loader ts-node/esm src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext ts"
  },
  "dependencies": {
    "fastify": "^4.24.0",
    "redis": "^4.6.0",
    "zod": "^3.22.0",
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  }
}
