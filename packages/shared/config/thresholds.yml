# Hover 실시간 개입 시스템 — 임계값 설정
# CHANGELOG:
# 2026-05-13 init: BE-C 초기값 설정 (임시)
# 2026-05-15 W2:   Retailrocket EDA §1.3 결과 반영
#                  tab_hidden_seconds 30→10 (전환율 곡선 변곡점)
#                  session_length_5min 0.1→0.25 (lift=32.19x, cap 0.25 적용)

scenarios:
  S1:
    base_match:
      cart_min_count: 1
      tab_hidden_seconds: 10   # Retailrocket §1.3 전환율 곡선 변곡점 (30→10)
    intent_score_min: 0.6      # ← W4 PR #2에서 갱신
    cooldown_seconds: 86400

booster_weights:
  clipboard_copy_match: 0.4        # 도메인 직관 (데이터 없음)
  broadcast_channel_multi_tab: 0.4 # 도메인 직관 (데이터 없음)
  referrer_price_compare: 0.2      # 도메인 직관 (GA4 분석 예정)
  session_length_5min: 0.25        # Retailrocket lift=32.19x, cap 0.25 적용
  hidden_repeated: 0.1             # ← W4 PR #2에서 갱신 (item_revisit 상관관계)
